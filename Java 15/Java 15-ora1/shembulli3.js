	// Storing number, boolean, strings in an Array
	var makina = ["BMW", 13000, "Range Rover", 20000, true];
	document.writeln(makina)

   //Getting the element from makina array
    document.write(makina[1]);
    document.write(makina[4]);

	var array = ["hi", 25000, "hello", 50000, "hey", true];
	// len contains the length of the array
	var len = array.length;
	for (var i = 0; i < len; i++){
		document.writeln(array[i]);
    }


