	
    
    function operator() {
	// multiple Conditional operators

	let marks = 95;
	let result = (marks < 49) ? "Arritje e kufizuar" :
			     (marks < 60) ? "Arritje mesatare" :
			     (marks < 80) ? "Arritje e lartë" : "Arritje e plotë" ;

	document.write(result);
	}
	operator();

    